services:
  bot:
    image: ghcr.io/dragonpaw/discord-bot:latest
    restart: unless-stopped
    network_mode: host
    environment:
      - BOT_TOKEN
      - CLIENT_ID
    volumes:
      - bot-state:/app/state

volumes:
  bot-state:
